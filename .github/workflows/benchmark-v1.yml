name: Benchmark V1

on:
  pull_request:
    paths:
      - "scripts/run_v1_benchmark.py"
      - "scripts/run_v12_weather_validation.py"
      - "scripts/run_v12_integration_phase2.py"
      - "scripts/run_v12_edge_validation.py"
      - "scripts/run_v12_under_signal_analysis.py"
      - "scripts/run_v12_grade_validation.py"
      - "scripts/run_v12_directional_grade_validation.py"
      - "oak_nfl/**"
      - ".github/workflows/benchmark-v1.yml"
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install package
        run: python -m pip install --upgrade pip && pip install -e ".[dev]"

      - name: Run Oak Baseline V1 benchmark
        run: python scripts/run_v1_benchmark.py

      - name: Run V12 weather validation
        run: python scripts/run_v12_weather_validation.py

      - name: Run V12 integration phase 2
        run: python scripts/run_v12_integration_phase2.py

      - name: Run V12 integrated edge validation
        run: python scripts/run_v12_edge_validation.py

      - name: Run V12 under signal robustness analysis
        run: python scripts/run_v12_under_signal_analysis.py

      - name: Run V12 confirmation score grade validation
        run: python scripts/run_v12_grade_validation.py

      - name: Run V12 direction-aware grade validation
        run: python scripts/run_v12_directional_grade_validation.py
