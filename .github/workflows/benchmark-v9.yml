name: Benchmark V9

on:
  pull_request:
    paths:
      - "scripts/run_v9_benchmark.py"
      - "oak_nfl/qb*.py"
      - ".github/workflows/benchmark-v9.yml"
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install --upgrade pip && pip install -e ".[dev]"
      - name: Run Oak V9 QB-change benchmark
        run: python scripts/run_v9_benchmark.py
